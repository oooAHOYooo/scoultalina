// TODO: Replace with real map and data fetching
document.addEventListener('DOMContentLoaded', () => {
  const map = document.getElementById('map');
  if (map) {
    map.innerText = 'Map will render here.';
  }
});


